# Database (remplace par tes vraies credentials PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/ft_transcendance?schema=public"

# JWT Secret (génère le tien avec : node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET="ton_secret_jwt_ultra_securise_change_moi"

# OAuth 42 (va sur https://profile.intra.42.fr/oauth/applications)
FT_CLIENT_ID="ton_client_id_42"
FT_CLIENT_SECRET="ton_client_secret_42"
FT_CALLBACK_URL="http://localhost:3000/api/auth/42/callback"

# Server
PORT=3000
NODE_ENV="development"

# Frontend URL (pour CORS)
FRONTEND_URL="http://localhost:5173"
